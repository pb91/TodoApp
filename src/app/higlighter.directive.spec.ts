import { HiglighterDirective } from './higlighter.directive';

describe('HiglighterDirective', () => {
  it('should create an instance', () => {
    const directive = new HiglighterDirective();
    expect(directive).toBeTruthy();
  });
});
